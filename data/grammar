#{
use lexer::{Token, Other, Accept_, Term, NTerm};
},
#Term(uint)
NTerm(uint) {
	{ Ok(0u) },
	NTerm 'c' => { let (inner, _) = symbols; Ok(1+inner) },
	NTerm Term => { let (inner, val) = symbols; Ok(inner+val) }
}
	