#Identifier(String)
#Fn


RustFile{
	{Ok(())},
	RustFile Function {Ok(())}
}

Function {
	Fn Identifier '(' Parameters ')' {Ok(())}
}

Parameters {
	{Ok(())}
	Parameters ',' Pattern {Ok(())}
}

Pattern {
	PatternInner {Ok(())}
	PatternInner ':' Type {Ok(())}
}

PatternInner {
	Identifier {Ok(())}
}

Type {
	Identifier {Ok(())}
}